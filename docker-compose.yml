version: '3.9'

services:
  web:
    build: .
    container_name: bug-tracker-app

    ports:
      - "5000:5000"

    environment:
      - FLASK_ENV=production

    volumes:
      - ./instance:/app/instance

    restart: unless-stopped